[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gateway-pink"
version = "0.1.0"
description = "Dependency-free Python client for Gateway.pink"
requires-python = ">=3.9"
